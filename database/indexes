CREATE INDEX customer_index
ON customer (email);

CREATE INDEX driver_index
ON driver (NIC);

CREATE INDEX driver_assis_index
ON driver_assistant (NIC);

CREATE INDEX order_index
ON `order_` (order_id);

CREATE INDEX order_includes_index
ON order_includes (order_id);

CREATE INDEX product_index
ON product (product_id);
